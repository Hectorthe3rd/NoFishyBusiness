# Website function errors








# Startup errors

start.bat doesn't appear to do anything, it returns the following:

"Suggestion [3,General]: The command start.bat was not found, but does exist in the current location. Windows PowerShell does not load commands from the current location by default. If you trust this command, instead type: ".\start.bat". See "get-help about_Command_Precedence" for more details."

So after typing .\start.bat into the terminal, it brings the following errors, provided in the document "TerminalErrors.txt"

Along with the fix to these errors, there are some items I'd like for you to do:

- Change the README.md startup to include other Operating systems besides windows
- Go through the eval, frontend, and backend codebases to add comments explaining that each line does - specially frontend as there are some changes to the web design format that I'd like to implement



# 



